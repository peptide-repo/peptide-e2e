name: CI Bisect Caller v2

on:
  push:
    branches: ['claude/**']

# NO permissions block — testing if job-level write in called workflow works

jobs:
  ci:
    uses: ./.github/workflows/ci-bisect.yml
    with:
      tests: none
      enforce_300: false
    secrets: inherit
