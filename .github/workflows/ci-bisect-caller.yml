name: CI Bisect Caller v3

on:
  push:
    branches: ['claude/**']

permissions:
  contents: write  # needed for called reusable workflow's phpcs job

jobs:
  ci:
    uses: ./.github/workflows/ci-bisect.yml
    with:
      tests: none
    secrets: inherit
