name: CI Bisect Caller

on:
  push:
    branches: ['claude/**']

permissions:
  contents: write

jobs:
  ci:
    uses: ./.github/workflows/ci-bisect.yml
    with:
      tests: none
      enforce_300: false
    secrets: inherit
